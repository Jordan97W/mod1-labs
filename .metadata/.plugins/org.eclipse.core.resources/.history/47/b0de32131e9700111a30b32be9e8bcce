package labop;

public class AirportKiosk {

	public static void main(String[] args) {
	
		double amount = 100;
		String customerType = "VIP";
		String currency = "EUR";
		double outputAmount = 0.0;
		
		switch(currency) {
		  case "USD":
			  outputAmount = amount * 1.25;
		    break;
		  case "EUR":
			  outputAmount = amount * 1.15;
		    break;
		  case "JPY":
			  outputAmount = amount * 180.50;
			 break;
		  default:
		    System.out.println("Invalid currency selected!");
			 break;
		}
		
		if(amount < 100.00) {
			
			outputAmount =  outputAmount * 1.05;
			
		}else{
			
			outputAmount =  outputAmount * 1.025;
		}
		
		if(customerType == "Regular") {
			
			outputAmount =  outputAmount * 1.1;
			
		}else if(customerType == "VIP") {
			
			outputAmount =  outputAmount * 1.05;
			
		}else if(customerType == "Staff") {
			
			
		}
		else {
			
			System.out.println("Invalid customer type!");
		}
		
		System.out.println(outputAmount);

	}

}
